name: workshop-app
services:
  - name: web-applicationhow
    git:
      repo_clone_url: https://github.com/fvcastellanos/workshop-app.git
      branch: feature/WE-29
    dockerfile_path: docker/Dockerfile
    run_command: java -Xms128M -Xmx128M -jar workshop-api.jar
    environment_slug: docker
    instance_size_slug: basic-xxs
    instance_count: 1
    routes:
      - path: /
